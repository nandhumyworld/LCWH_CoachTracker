# Normalize line endings to LF in the repo for all text files. Prevents
# CRLF churn between Windows dev and the Linux Docker build.
* text=auto eol=lf

# Binary types Git should never touch.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
